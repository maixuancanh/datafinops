import type { FastifyInstance } from 'fastify';
import { appendApproval, authorizeApproval } from '@datafinops/domain';
export function registerApprovalRoutes(app: FastifyInstance): void { app.post('/v1/approvals', async (request, reply) => { try { const body = (request.body ?? {}) as Record<string, unknown>; authorizeApproval({ approverId: String(body.approverId ?? ''), role: String(body.role ?? ''), requiredRole: String(body.requiredRole ?? ''), selfApproval: Boolean(body.selfApproval), expired: Boolean(body.expired) }); return reply.code(201).send({ approval: appendApproval({ ...body, id: `approval_${Date.now()}`, decision: body.decision === 'REJECTED' ? 'REJECTED' : 'APPROVED', createdAt: new Date().toISOString() } as never) }); } catch (error) { return reply.code(403).send({ error: error instanceof Error ? error.message : 'approval denied' }); } }); }
