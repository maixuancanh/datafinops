export interface EntitlementExpectation { readonly subscriptionId: string; readonly network: string; readonly programId: string; readonly proposalHash: string; readonly periodStart: string; readonly periodEnd: string; readonly league?: string; readonly fixtureId?: string; readonly maxLatencyMs?: number; }
export interface EntitlementObserved extends EntitlementExpectation { readonly latencyMs?: number; }
export function verifyEntitlement(expected: EntitlementExpectation, observed: Partial<EntitlementObserved>): { readonly status: 'VERIFIED' | 'MISMATCH'; readonly reasons: readonly string[] } { const keys: (keyof EntitlementExpectation)[] = ['subscriptionId', 'network', 'programId', 'proposalHash', 'periodStart', 'periodEnd', 'league', 'fixtureId']; const reasons = keys.filter((key) => observed[key] !== undefined && observed[key] !== expected[key]).map((key) => `${String(key).toUpperCase()}_MISMATCH`); if (expected.maxLatencyMs !== undefined && observed.latencyMs !== undefined && observed.latencyMs > expected.maxLatencyMs) reasons.push('LATENCY_MISMATCH'); return { status: reasons.length ? 'MISMATCH' : 'VERIFIED', reasons }; }
