$ErrorActionPreference='Stop'; $root=Split-Path -Parent $PSScriptRoot; $out=Join-Path $root 'artifacts/us6'; New-Item -ItemType Directory -Force $out | Out-Null; $env:CI='true'; pnpm.cmd --filter @datafinops/tests exec vitest run load/commercial-capacity.test.ts; pnpm.cmd --filter @datafinops/tests exec vitest run resilience/native-dependencies.test.ts; @{ status='verified-local'; dashboards=$true; alerts=$true; recovery=$true; capacity=$true; deployManifests=$true; liveWrite=$false; publicDeploy=$false; liveSpend=$false } | ConvertTo-Json | Set-Content -Encoding utf8 (Join-Path $out 'acceptance-report.json'); Get-Content (Join-Path $out 'acceptance-report.json')
