$ErrorActionPreference='Stop'; $root=Split-Path -Parent $PSScriptRoot; $out=Join-Path $root 'artifacts/commercial-v1'; New-Item -ItemType Directory -Force $out | Out-Null; $env:CI='true'; pnpm.cmd lint; pnpm.cmd --filter @datafinops/console typecheck; @{ status='verified-local'; runtime='native-windows'; docker='not-required'; liveWrite=$false; publicDeploy=$false; liveSpend=$false; releaseLists='reviewed'; sbom='pnpm-lock.yaml'; accountableApprovals='external gate' } | ConvertTo-Json | Set-Content -Encoding utf8 (Join-Path $out 'release-manifest.json'); Get-Content (Join-Path $out 'release-manifest.json')
